# .NET
bin/
obj/
*.user
.vs/

# Node / Next.js
node_modules/
.next/
out/

# Ambiente e segredos — NUNCA commitar
.env
.env.*
!.env.example
appsettings.Development.json
appsettings.*.Local.json
*.pfx
*.key

# IDE / OS
.idea/
.DS_Store
Thumbs.db

# Claude Code — definições locais da máquina
.claude/settings.local.json
# Config local de arranque (contém args com credenciais de dev)
.claude/launch.json

# Dados locais
docker-volumes/
*.db

# Dumps de base de dados — nunca versionados (docs/05-git-workflow.md).
# Contêm dados pessoais (nomes, datas de nascimento). O *.dump é defesa em
# profundidade para o caso de um dump ser escrito fora de backups/.
backups/
*.dump
